<script lang="ts">
	import type { System } from "../../../../system/class";
	import type { Card } from "../../../class";

	// svelte-ignore export_let_unused
	export let system: System;
	// svelte-ignore export_let_unused
	export let card: Card;
</script>

Quand posé : augmente de 10 sa constitution et sa force pour chaque famille différentes parmi les créatures alliées sur le terrain.
